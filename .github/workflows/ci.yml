name: CI

# Run the test suite on pushes and pull requests to the protected branches.
# Merges into develop/main are gated on this "test" job passing.
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle          # cache ~/.gradle so builds are fast

      - name: Run tests
        run: ./gradlew test --no-daemon

      - name: Upload test report
        if: always()             # keep the HTML report even when tests fail
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: build/reports/tests/test
          if-no-files-found: ignore
